export class Human
{
  public weight : number;
  public spriteKey : string;  

  constructor(weight : number, spriteKey : string)
  {
      this.weight = weight;
      this.spriteKey = spriteKey;
  }
}